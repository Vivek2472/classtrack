/**
 * ClassTrack Supabase Configuration (Template)
 * 
 * Instructions:
 * 1. Copy this file to `js/config.js`
 * 2. Replace the placeholder values with your Supabase Project URL and Anon Public Key.
 * 3. `js/config.js` is included in `.gitignore` to avoid committing personal credentials to GitHub.
 * 
 * NOTE: The `anon` key is designed for browser use with Row Level Security (RLS).
 * NEVER expose your `service_role` admin key!
 */

window.CLASSTRACK_SUPABASE_CONFIG = {
  SUPABASE_URL: '',      // Example: 'https://xyzcompany.supabase.co'
  SUPABASE_ANON_KEY: ''  // Example: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
};
